{
    "file_name": "NameCaseConverter",
    "package": "com.conversion.abh001",
    "content": "package com.conversion.abh001;\n\n/**\n * Utility class to convert names to lower or upper case based on the provided flag.\n */\npublic class NameCaseConverter {\n\n    /**\n     * Converts the given name to lower or upper case based on the flag.\n     * \n     * @param name  The name to be converted\n     * @param flag  The flag indicating the conversion type. 'l' or 'L' for lower case, 'u' or 'U' for upper case.\n     * @return The converted name\n     */\n    public static String convertName(String name, String flag) {\n        if (flag == null) {\n            throw new IllegalArgumentException(\"Flag cannot be null\");\n        }\n\n        char flagChar = flag.charAt(0);\n        if (Character.toLowerCase(flagChar) == 'l') {\n            return name.toLowerCase();\n        } else if (Character.toLowerCase(flagChar) == 'u') {\n            return name.toUpperCase();\n        } else {\n            // If the flag is neither 'l' nor 'u', return the original name\n            return name;\n        }\n    }\n}",
    "description": "A utility class that converts a name to lower or upper case based on a provided flag."
}